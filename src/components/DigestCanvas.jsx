import []

import page_styles from "../modules/DigestCanvas.module.scss";


function DigestCanvas ({ onEdit = () => false, setEditMode = f => f}) {
	const 
	return (
		<canvas id = "digestCanvas">Your Browser too old!</canvas>
	);
}
